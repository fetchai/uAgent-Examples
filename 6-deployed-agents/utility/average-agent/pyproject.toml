[project]
name = "average-agent"
version = "0.1.0"
description = "He's just an average agent and a bit mean."
authors = [
  { name = "Florian Wilde", email = "florian.wilde@fetch.ai" }
]
readme = "README.md"
requires-python = ">=3.10,<3.13"
dependencies = [
  "uagents>=0.25.4",
  "uagents-core>=0.4.9"
]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
